export enum LinePattern {
  SOLID = 'solid',
  DASHED = 'dashed',
  DOTTED = 'dotted',
  DISCONTINUOUS = 'discontinuous',
}

export enum ArrowHeadType {
  NONE = 'none',
  TRIANGLE = 'triangle',
  CIRCLE = 'circle',
  SQUARE = 'square',
  BAR = 'bar',
}

export enum EditorElementKind {
  ASSET = 'asset',
  TEXT = 'text',
  ARROW = 'arrow',
  LINE = 'line',
  DRAW = 'draw',
  RECT = 'rect',
  CIRCLE = 'circle',
}
